[project]
name = "financial-data-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp[auth]>=4.0.3",
    "yfinance>=1.7.0",
]
